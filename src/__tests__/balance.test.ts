/**\n * 밸런스 계산 유닛 테스트\n */\n\nimport { calculateMonsterHp, calculateMonsterReward, calculateUpgradeCost } from '@/utils/balance';\n\ndescribe('Balance Calculations', () => {\n  // 몬스터 HP 계산\n  describe('calculateMonsterHp', () => {\n    it('should calculate HP for stage 1', () => {\n      const hp = calculateMonsterHp(1);\n      expect(hp).toBe(50); // 기본값\n    });\n\n    it('should increase HP gradually for higher stages', () => {\n      const hp1 = calculateMonsterHp(1);\n      const hp10 = calculateMonsterHp(10);\n      const hp50 = calculateMonsterHp(50);\n\n      expect(hp10).toBeGreaterThan(hp1);\n      expect(hp50).toBeGreaterThan(hp10);\n    });\n\n    it('should handle boss stages with 2x multiplier', () => {\n      const normalHp = calculateMonsterHp(9);\n      const bossHp = calculateMonsterHp(10);\n\n      expect(bossHp).toBeGreaterThan(normalHp * 1.5); // 보스는 훨씬 강함\n    });\n  });\n\n  // 몬스터 보상 계산\n  describe('calculateMonsterReward', () => {\n    it('should calculate reward for stage 1', () => {\n      const reward = calculateMonsterReward(1);\n      expect(reward).toBeGreaterThan(0);\n    });\n\n    it('should increase reward for higher stages', () => {\n      const reward1 = calculateMonsterReward(1);\n      const reward10 = calculateMonsterReward(10);\n      const reward50 = calculateMonsterReward(50);\n\n      expect(reward10).toBeGreaterThan(reward1);\n      expect(reward50).toBeGreaterThan(reward10);\n    });\n\n    it('should give bonus reward for boss stages', () => {\n      const normalReward = calculateMonsterReward(9);\n      const bossReward = calculateMonsterReward(10);\n\n      expect(bossReward).toBeGreaterThan(normalReward * 1.5);\n    });\n  });\n\n  // 업그레이드 비용 계산\n  describe('calculateUpgradeCost', () => {\n    it('should calculate cost for level 0', () => {\n      const cost = calculateUpgradeCost('atk', 0);\n      expect(cost).toBeGreaterThan(0);\n    });\n\n    it('should increase cost exponentially', () => {\n      const cost0 = calculateUpgradeCost('atk', 0);\n      const cost1 = calculateUpgradeCost('atk', 1);\n      const cost5 = calculateUpgradeCost('atk', 5);\n\n      expect(cost1).toBeGreaterThan(cost0);\n      expect(cost5).toBeGreaterThan(cost1);\n    });\n\n    it('should handle different upgrade types', () => {\n      const atkCost = calculateUpgradeCost('atk', 0);\n      const hpCost = calculateUpgradeCost('hp', 0);\n\n      expect(atkCost).toBeGreaterThan(0);\n      expect(hpCost).toBeGreaterThan(0);\n    });\n  });\n});\n
