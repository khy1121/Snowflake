/**\n * 동료 커스터마이징 엔진\n * Sprint 10: 동료 외형/호칭/프로필 커스터마이징\n */\n\nimport { CompanionCustomization, AuraStyle, OutlineStyle } from '../../ugc/types';\n\n/**\n * 기본 커스터마이징 생성\n */\nexport function createDefaultCustomization(companionId: string): CompanionCustomization {\n  return {\n    companionId,\n    nickname: '', // 기본값 없음 (동료 원래 이름 사용)\n    auraStyle: 'NONE',\n    outlineStyle: 'NONE',\n    colorPalette: 'default',\n  };\n}\n\n/**\n * 커스터마이징 업데이트\n */\nexport function updateCustomization(\n  customization: CompanionCustomization,\n  updates: Partial<CompanionCustomization>\n): CompanionCustomization {\n  return {\n    ...customization,\n    ...updates,\n    companionId: customization.companionId, // ID는 변경 불가\n  };\n}\n\n/**\n * 닉네임 설정\n */\nexport function setNickname(customization: CompanionCustomization, nickname: string): CompanionCustomization {\n  // 0~12자 제한\n  const trimmed = nickname.substring(0, 12);\n  return { ...customization, nickname: trimmed };\n}\n\n/**\n * 오라 스타일 설정\n */\nexport function setAuraStyle(customization: CompanionCustomization, auraStyle: AuraStyle): CompanionCustomization {\n  return { ...customization, auraStyle };\n}\n\n/**\n * 테두리 스타일 설정\n */\nexport function setOutlineStyle(customization: CompanionCustomization, outlineStyle: OutlineStyle): CompanionCustomization {\n  return { ...customization, outlineStyle };\n}\n\n/**\n * 색상 팔레트 설정\n */\nexport function setColorPalette(customization: CompanionCustomization, colorPalette: string): CompanionCustomization {\n  return { ...customization, colorPalette };\n}\n\n/**\n * 뱃지/칭호 설정\n */\nexport function setBadge(customization: CompanionCustomization, badgeId: string | undefined): CompanionCustomization {\n  return { ...customization, badgeId };\n}\n\n/**\n * 커스터마이징 리셋\n */\nexport function resetCustomization(companionId: string): CompanionCustomization {\n  return createDefaultCustomization(companionId);\n}\n\n/**\n * 오라 스타일 설명\n */\nexport function getAuraStyleDescription(auraStyle: AuraStyle, language: 'ko' | 'en' = 'ko'): string {\n  const descriptions = {\n    ko: {\n      NONE: '오라 없음',\n      SOFT_GLOW: '부드러운 빛',\n      BRIGHT_HALO: '밝은 후광',\n      DARK_AURA: '어두운 기운',\n    },\n    en: {\n      NONE: 'No Aura',\n      SOFT_GLOW: 'Soft Glow',\n      BRIGHT_HALO: 'Bright Halo',\n      DARK_AURA: 'Dark Aura',\n    },\n  };\n  return descriptions[language][auraStyle] || auraStyle;\n}\n\n/**\n * 테두리 스타일 설명\n */\nexport function getOutlineStyleDescription(outlineStyle: OutlineStyle, language: 'ko' | 'en' = 'ko'): string {\n  const descriptions = {\n    ko: {\n      NONE: '테두리 없음',\n      THIN: '얇은 테두리',\n      THICK: '두꺼운 테두리',\n      NEON: '네온 테두리',\n    },\n    en: {\n      NONE: 'No Outline',\n      THIN: 'Thin Outline',\n      THICK: 'Thick Outline',\n      NEON: 'Neon Outline',\n    },\n  };\n  return descriptions[language][outlineStyle] || outlineStyle;\n}\n\n/**\n * 색상 팔레트 정의\n */\nexport const COLOR_PALETTES = {\n  default: { primary: '#FF6B6B', secondary: '#4ECDC4', accent: '#FFE66D' },\n  cool: { primary: '#4A90E2', secondary: '#50C878', accent: '#9B59B6' },\n  warm: { primary: '#FF8C42', secondary: '#FF6B35', accent: '#F7931E' },\n  pastel: { primary: '#FFB3BA', secondary: '#BAFFC9', accent: '#BAE1FF' },\n  dark: { primary: '#2C3E50', secondary: '#34495E', accent: '#E74C3C' },\n  neon: { primary: '#FF006E', secondary: '#00F5FF', accent: '#FFBE0B' },\n};\n\n/**\n * 색상 팔레트 가져오기\n */\nexport function getColorPalette(paletteId: string) {\n  return (COLOR_PALETTES as any)[paletteId] || COLOR_PALETTES.default;\n}\n\n/**\n * 사용 가능한 팔레트 목록\n */\nexport function getAvailablePalettes() {\n  return Object.keys(COLOR_PALETTES);\n}\n
