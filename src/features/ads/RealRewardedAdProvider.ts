/**\n * 실제 광고 제공자 (Google Mobile Ads)\n * 확장 포인트: 실제 SDK 연동 시 이 파일을 구현\n */\n\nimport { IRewardedAdProvider, RewardedAdResult, AdPlacementId } from './RewardedAdProvider';\n\n/**\n * Google Mobile Ads를 사용한 실제 광고 제공자\n * 주석: 실제 구현 시 다음과 같이 구현할 수 있습니다.\n *\n * import { RewardedInterstitialAd, RewardedAdEventType } from 'react-native-google-mobile-ads';\n *\n * export class GoogleRewardedAdProvider implements IRewardedAdProvider {\n *   private adUnitIds: Record<AdPlacementId, string> = {\n *     OFFLINE_DOUBLE: 'ca-app-pub-xxxxxxxxxxxxxxxx/yyyyyyyyyy',\n *     BONUS_CHEST: 'ca-app-pub-xxxxxxxxxxxxxxxx/yyyyyyyyyy',\n *     PASS_XP_BOOST: 'ca-app-pub-xxxxxxxxxxxxxxxx/yyyyyyyyyy',\n *   };\n *\n *   async showRewardedAd(placementId: AdPlacementId): Promise<RewardedAdResult> {\n *     try {\n *       const adUnitId = this.adUnitIds[placementId];\n *       const rewardedInterstitialAd = await RewardedInterstitialAd.load(adUnitId, {\n *         requestNonPersonalizedAdsOnly: true,\n *       });\n *\n *       return new Promise((resolve) => {\n *         rewardedInterstitialAd.onAdEvent((type) => {\n *           if (type === RewardedAdEventType.EARNED_REWARD) {\n *             resolve({\n *               success: true,\n *               placementId,\n *               timestamp: Date.now(),\n *             });\n *           } else if (type === RewardedAdEventType.CLOSED) {\n *             resolve({\n *               success: false,\n *               placementId,\n *               timestamp: Date.now(),\n *             });\n *           }\n *         });\n *\n *         rewardedInterstitialAd.show();\n *       });\n *     } catch (error) {\n *       console.error('Failed to show rewarded ad:', error);\n *       return {\n *         success: false,\n *         placementId,\n *         timestamp: Date.now(),\n *       };\n *     }\n *   }\n *\n *   async isAdAvailable(placementId: AdPlacementId): Promise<boolean> {\n *     // 실제 구현: 광고 로드 상태 확인\n *     return true;\n *   }\n *\n *   async preloadAd(placementId: AdPlacementId): Promise<void> {\n *     try {\n *       const adUnitId = this.adUnitIds[placementId];\n *       await RewardedInterstitialAd.load(adUnitId, {\n *         requestNonPersonalizedAdsOnly: true,\n *       });\n *     } catch (error) {\n *       console.error('Failed to preload ad:', error);\n *     }\n *   }\n * }\n */\n\n/**\n * 실제 광고 제공자 설정 가이드\n *\n * 1. 패키지 설치:\n *    npm install react-native-google-mobile-ads\n *\n * 2. app.json 설정:\n *    {\n *      \"plugins\": [\n *        [\n *          \"react-native-google-mobile-ads\",\n *          {\n *            \"appId\": \"ca-app-pub-xxxxxxxxxxxxxxxx~yyyyyyyyyy\"\n *          }\n *        ]\n *      ]\n *    }\n *\n * 3. 초기화:\n *    import { mobileAds } from 'react-native-google-mobile-ads';\n *    mobileAds().initialize();\n *\n * 4. 광고 단위 ID 등록:\n *    - Google AdMob 콘솔에서 앱 등록\n *    - 각 배치별 광고 단위 생성\n *    - 광고 단위 ID를 adUnitIds에 설정\n *\n * 5. 테스트 기기 설정:\n *    mobileAds().setRequestConfiguration({\n *      testDeviceIdentifiers: ['33BE2250B43518CCDA7DE426D04EE232'],\n *    });\n */\n\nexport class RealRewardedAdProviderGuide {\n  static getImplementationGuide(): string {\n    return `\n# 실제 광고 제공자 구현 가이드\n\n## 1. 사전 준비\n- Google AdMob 계정 생성 (https://admob.google.com)\n- 앱 등록 및 광고 단위 생성\n\n## 2. 패키지 설치\n\\`\\`\\`bash\nnpm install react-native-google-mobile-ads\n\\`\\`\\`\n\n## 3. 광고 단위 ID 설정\nRealRewardedAdProvider의 adUnitIds에 다음을 설정:\n- OFFLINE_DOUBLE: 오프라인 보상 2배 광고\n- BONUS_CHEST: 보너스 상자 광고\n- PASS_XP_BOOST: 패스 XP 부스트 광고\n\n## 4. 초기화\nApp.tsx에서 다음과 같이 초기화:\n\\`\\`\\`typescript\nimport { mobileAds } from 'react-native-google-mobile-ads';\nimport { setRewardedAdProvider } from '@/features/ads/RewardedAdProvider';\nimport { GoogleRewardedAdProvider } from '@/features/ads/RealRewardedAdProvider';\n\n// 초기화\nmobileAds().initialize();\n\n// 실제 광고 제공자 설정\nif (!__DEV__) {\n  setRewardedAdProvider(new GoogleRewardedAdProvider());\n}\n\\`\\`\\`\n\n## 5. 테스트\n- 테스트 기기에서 광고 표시 확인\n- 광고 클릭 후 보상 지급 확인\n    `;\n  }\n}\n
