/**\n * 접근성(Accessibility) 설정 서비스\n * 시니어 친화 + 장애인 접근성\n */\n\nexport interface AccessibilitySettings {\n  // 글자 크기\n  fontScale: number; // 1.0 = 기본, 1.5 = 50% 크게, 2.0 = 2배\n  \n  // 색상 대비\n  highContrast: boolean; // 고대비 모드\n  colorBlindMode: 'none' | 'deuteranopia' | 'protanopia' | 'tritanopia';\n  \n  // 모션\n  reduceMotion: boolean; // 파티클/흔들림 최소화\n  reduceVibration: boolean; // 진동 비활성화\n  \n  // 숫자 표기\n  numberFormat: 'compact' | 'comma'; // K/M/B vs 1,234,567\n  \n  // 음성\n  textToSpeech: boolean; // 텍스트 음성 변환\n}\n\n/**\n * 기본 접근성 설정\n */\nexport const DEFAULT_ACCESSIBILITY_SETTINGS: AccessibilitySettings = {\n  fontScale: 1.0,\n  highContrast: false,\n  colorBlindMode: 'none',\n  reduceMotion: false,\n  reduceVibration: false,\n  numberFormat: 'compact',\n  textToSpeech: false,\n};\n\n/**\n * 싱글톤 인스턴스\n */\nlet accessibilitySettings: AccessibilitySettings = { ...DEFAULT_ACCESSIBILITY_SETTINGS };\n\n/**\n * 접근성 설정 조회\n */\nexport function getAccessibilitySettings(): AccessibilitySettings {\n  return { ...accessibilitySettings };\n}\n\n/**\n * 접근성 설정 업데이트\n */\nexport function updateAccessibilitySettings(\n  updates: Partial<AccessibilitySettings>\n): void {\n  accessibilitySettings = { ...accessibilitySettings, ...updates };\n}\n\n/**\n * 글자 크기 계산\n */\nexport function calculateFontSize(baseFontSize: number): number {\n  return baseFontSize * accessibilitySettings.fontScale;\n}\n\n/**\n * 숫자 포맷팅\n */\nexport function formatNumber(value: number): string {\n  if (accessibilitySettings.numberFormat === 'comma') {\n    return value.toLocaleString('ko-KR');\n  }\n\n  // Compact 포맷\n  if (value >= 1_000_000_000) {\n    return (value / 1_000_000_000).toFixed(1) + 'B';\n  }\n  if (value >= 1_000_000) {\n    return (value / 1_000_000).toFixed(1) + 'M';\n  }\n  if (value >= 1_000) {\n    return (value / 1_000).toFixed(1) + 'K';\n  }\n  return value.toString();\n}\n\n/**\n * 색상 변환 (색맹 모드)\n */\nexport function transformColor(color: string): string {\n  if (accessibilitySettings.colorBlindMode === 'none') {\n    return color;\n  }\n\n  // 간단한 색상 변환 (실제로는 더 정교한 알고리즘 필요)\n  // 여기서는 플레이스홀더\n  return color;\n}\n\n/**\n * 모션 효과 활성화 여부\n */\nexport function shouldReduceMotion(): boolean {\n  return accessibilitySettings.reduceMotion;\n}\n\n/**\n * 진동 활성화 여부\n */\nexport function shouldVibrate(): boolean {\n  return !accessibilitySettings.reduceVibration;\n}\n\n/**\n * 고대비 모드 활성화 여부\n */\nexport function isHighContrastMode(): boolean {\n  return accessibilitySettings.highContrast;\n}\n\n/**\n * 텍스트 음성 변환 활성화 여부\n */\nexport function isTextToSpeechEnabled(): boolean {\n  return accessibilitySettings.textToSpeech;\n}\n\n/**\n * 접근성 설정 초기화\n */\nexport function resetAccessibilitySettings(): void {\n  accessibilitySettings = { ...DEFAULT_ACCESSIBILITY_SETTINGS };\n}\n
