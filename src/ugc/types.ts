/**\n * UGC (User-Generated Content) - 정비 의뢰(Scenario) 타입 정의\n * Sprint 10: 커뮤니티 기반 콘텐츠 확장\n */\n\nexport type DifficultyTag = 'EASY' | 'NORMAL' | 'HARD';\nexport type RewardMode = 'NONE' | 'CAPPED';\nexport type ModifierType = 'SHIELD_PHASE' | 'RAGE_PHASE' | 'FAST_ENEMY' | 'SLOW_PLAYER' | 'NO_CRIT' | 'GOLD_REDUCED';\nexport type AuraStyle = 'NONE' | 'SOFT_GLOW' | 'BRIGHT_HALO' | 'DARK_AURA';\nexport type OutlineStyle = 'NONE' | 'THIN' | 'THICK' | 'NEON';\n\n/**\n * 시나리오 내 스테이지 정의\n */\nexport interface UGCStage {\n  stageIndex: number; // 0부터 시작\n  enemyPresetId: string; // 기존 몬스터 타입 참조 (e.g., \"goblin\", \"boss_dragon\")\n  hpMultiplier: number; // 1.0 ~ 5.0\n  rewardMultiplier: number; // 0.0 ~ 1.0 (기본 0.0 권장)\n  modifiers: ModifierType[]; // 최대 3개\n  isBoss?: boolean; // 보스 스테이지 여부\n}\n\n/**\n * 시나리오 내 대사 정의\n */\nexport interface UGCDialogue {\n  speaker: 'DOKKAEBI' | 'JANGSEUNG' | 'HAETAE' | 'ANONYMOUS' | 'PLAYER'; // 화자\n  text: string; // 대사 (0~200자)\n  triggerStageIndex?: number; // 특정 스테이지 후 표시 (미지정 시 시작 시)\n}\n\n/**\n * 시나리오 규칙\n */\nexport interface ScenarioRules {\n  maxOfflineMultiplierAllowed: boolean; // 항상 false\n  rewardMode: RewardMode; // 기본 NONE\n  dailyRewardCap: number; // 기본 0, CAPPED일 때만 1~3\n}\n\n/**\n * 정비 의뢰(Scenario) 메인 타입\n */\nexport interface UGCScenario {\n  id: string; // UUID\n  version: number; // 스키마 버전\n  title: string; // 2~24자\n  summary: string; // 0~80자\n  authorName: string; // 0~12자, 기본값 \"익명 정비사\"\n  language: 'ko' | 'en';\n  createdAt: number; // timestamp\n  updatedAt: number; // timestamp\n  difficultyTag: DifficultyTag;\n  stages: UGCStage[]; // 3~8개\n  dialogues?: UGCDialogue[]; // 0~12개\n  rules: ScenarioRules;\n  checksum: string; // export/import 무결성 검증용\n}\n\n/**\n * 동료 커스터마이징 정의\n */\nexport interface CompanionCustomization {\n  companionId: string; // 동료 ID\n  nickname: string; // 0~12자\n  auraStyle: AuraStyle;\n  outlineStyle: OutlineStyle;\n  colorPalette: string; // 팔레트 ID\n  badgeId?: string; // 뱃지/칭호 ID\n}\n\n/**\n * UGC 일일 보상 상태\n */\nexport interface UGCDailyCapState {\n  lastResetDate: string; // YYYY-MM-DD\n  playedCount: number; // 오늘 플레이한 횟수\n  rewardGiven: number; // 오늘 받은 보상\n}\n\n/**\n * UGC 신고 로그\n */\nexport interface UGCReport {\n  id: string; // UUID\n  scenarioId: string;\n  reason: 'PROFANITY' | 'INAPPROPRIATE' | 'FAKE_REWARD' | 'OTHER';\n  description: string; // 0~200자\n  reportedAt: number; // timestamp\n}\n\n/**\n * 피드백 로그\n */\nexport interface FeedbackLog {\n  id: string; // UUID\n  type: 'BUG' | 'SUGGESTION' | 'UGC_RELATED';\n  text: string; // 0~500자\n  attachedState?: string; // 진단 정보 (JSON 문자열)\n  submittedAt: number; // timestamp\n}\n\n/**\n * 진단 덤프\n */\nexport interface DiagnosticDump {\n  timestamp: number;\n  stage: number;\n  gold: number;\n  companionCount: number;\n  eventState: string;\n  experimentVariant: string;\n  language: string;\n  recentLogs: string[]; // 최근 30개 로그\n}\n
