# 🌟 Sprint 7: 라이브옵스 완성 업데이트 (v1.2)\n\n## (1) Sprint 7 목표 및 완료 조건\n\n### 목표\n\n**\"유저가 오래 남는 이유\"를 만든다**. Sprint 6까지 완성된 게임의 기본 루프와 콘텐츠를 바탕으로, 커뮤니티(친구, 정비반), 랭킹, 라이브옵스 커뮤니케이션(인앱 메시지), A/B 실험, 치트 방지 시스템을 추가하여 **사회적 상호작용, 경쟁 요소, 운영 유연성, 안정성**을 갖춘 완성도 높은 라이브 게임으로 진화시킵니다.\n\n### 완료 조건 (DoD)\n\n- ✅ **친구 시스템**: 친구 코드(8자리), 요청/수락/목록, 프로필(닉네임/스테이지/전투력) 동작\n- ✅ **정비반(그룹)**: 최대 10명, 주간 정비 포인트, 목표 달성 시 보상(토큰/칭호)\n- ✅ **주간 랭킹 2종**: 스테이지 랭킹, 정비 포인트 랭킹 + 주간 키 기반 자동 리셋\n- ✅ **이벤트 랭킹**: 이벤트 ON일 때만 표시, 토큰 누적 기준\n- ✅ **인앱 메시지**: 배너, 모달, 우편함 + 원격 설정 제어 + 닫힘 상태 저장\n- ✅ **A/B 실험 3종**: 튜토리얼, 오프라인 보상, 패스 XP + Sticky assignment + 로깅\n- ✅ **치트 방지**: Soft clamp, 비정상 값 탐지, 검증 강화 + 랭킹 제출 검증\n- ✅ **SaveState V7**: 친구 코드, 메시지 상태, 실험 배정, 주간 보상 클레임 저장\n- ✅ **운영 가이드**: 메시지/실험/랭킹 운영 방법, Firebase Rules(선택), 리셋 운영 방식\n\n---\n\n## (2) 변경된 파일 트리\n\n### 추가된 파일 (🆕)\n\n```\nsrc/data/\n├── friends.json                    🆕 친구 시스템 설정 (코드 포맷, 닉네임 템플릿)\n├── inAppMessages.json              🆕 인앱 메시지 기본 데이터\n└── experiments.json                🆕 A/B 실험 설정 (3종)\n\nsrc/features/social/\n├── friendsEngine.ts                🆕 친구 시스템 로직\n├── leaderboardEngine.ts            🆕 랭킹 시스템 로직\n├── FriendsScreen.tsx               🆕 친구 화면 UI\n├── LeaderboardScreen.tsx           🆕 랭킹 화면 UI\n└── GroupScreen.tsx                 🆕 정비반 화면 UI\n\nsrc/features/messaging/\n├── inAppMessageEngine.ts           🆕 인앱 메시지 로직\n└── InboxScreen.tsx                 🆕 우편함 화면 UI\n\nsrc/services/\n├── ExperimentService.ts            🆕 A/B 실험 서비스\n└── ValidationService.ts            🆕 치트 방지 및 검증 서비스\n\nsrc/features/save/\n└── migrationV7.ts                  🆕 SaveState 마이그레이션 (V6→V7)\n\ndocs/\n├── SPRINT7_SUMMARY.md              🆕 이 문서\n├── OPERATIONS_GUIDE.md             🆕 운영 가이드\n└── FIREBASE_SETUP.md               🆕 Firebase 설정 가이드 (선택)\n```\n\n### 수정된 파일 (📝)\n\n```\nsrc/\n├── types.ts                        📝 FriendRequest, UserProfile, LeaderboardEntry 등 타입 추가\n├── store.ts                        📝 zustand에 친구/랭킹/메시지/실험 상태 추가\n└── features/save/\n    ├── persistence.ts              📝 V7 마이그레이션 적용\n    └── migrationV7.ts              📝 Sprint 7 마이그레이션 로직\n\nsrc/app/\n├── App.tsx                         📝 새로운 탭(Social) 추가\n└── screens/\n    └── HomeScreen.tsx              📝 인앱 메시지 배너 표시\n\npackage.json                        📝 테스트 스크립트 업데이트\nRELEASE_CHECKLIST.md                📝 Sprint 7 회귀 테스트 시나리오 추가\n```\n\n---\n\n## (3) 핵심 파일 전체 코드\n\n### 📂 `src/features/social/friendsEngine.ts` (친구 시스템)\n\n**주요 함수**:\n\n```typescript\n// 친구 코드 생성 (8자리 대문자/숫자)\ngenerateFriendCode(): string\n\n// 친구 코드 유효성 확인\nisValidFriendCode(code: string): boolean\n\n// 친구 요청 생성\ncreateFriendRequest(userId: string, friendUserId: string): FriendRequest\n\n// 친구 요청 수락/거절\nacceptFriendRequest(request: FriendRequest): FriendRequest\nrejectFriendRequest(request: FriendRequest): FriendRequest\n\n// 친구 목록 조회\ngetAcceptedFriends(requests: FriendRequest[]): FriendRequest[]\n\n// 전투력 계산\ncalculatePower(atk: number, critChance?: number, critDamage?: number): number\n```\n\n### 📂 `src/features/social/leaderboardEngine.ts` (랭킹 시스템)\n\n**주요 함수**:\n\n```typescript\n// 주간 키 생성 (YYYY-WW)\ngetWeekKey(date?: Date): string\n\n// 랭킹 업데이트 (중복 제거, 정렬)\nupdateLeaderboard(entries, newEntry): LeaderboardEntry[]\n\n// 사용자 순위 조회\ngetUserRank(entries, userId): LeaderboardEntry | null\n\n// 상위 10% 보상 기준 확인\nisTopPercentile(rank, totalEntries, percentile?): boolean\n\n// 랭킹 보상 계산\ncalculateLeaderboardReward(rank, totalEntries): { cosmeticTokens, title? }\n\n// 주간 키 변경 확인 (자동 리셋)\nshouldResetLeaderboard(lastWeekKey): boolean\n```\n\n### 📂 `src/features/messaging/inAppMessageEngine.ts` (인앱 메시지)\n\n**주요 함수**:\n\n```typescript\n// 활성 메시지 필터링\ngetActiveMessages(messages, now?): InAppMessage[]\n\n// 메시지 타입별 필터링\ngetMessagesByType(messages, type): InAppMessage[]\n\n// 우선순위별 정렬\nsortByPriority(messages): InAppMessage[]\n\n// 메시지 읽음/닫음 표시\nmarkMessageAsRead(state): MessageState\ndismissMessage(state): MessageState\n\n// 표시할 메시지 결정\ngetMessagesToDisplay(messages, messageStates, now?): InAppMessage[]\n\n// 배너/모달/우편함 조회\ngetActiveBanners(messages, messageStates, now?): InAppMessage[]\ngetActiveModals(messages, messageStates, now?): InAppMessage[]\ngetInboxMessages(messages, messageStates, now?): InAppMessage[]\n```\n\n### 📂 `src/services/ExperimentService.ts` (A/B 실험)\n\n**주요 함수**:\n\n```typescript\n// Sticky assignment: 사용자별 고정 배정\nassignVariant(experimentKey, userId, variants, existingAssignment?): string\n\n// 실험 활성 여부 확인\nisExperimentActive(config, now?): boolean\n\n// 배정된 variant 조회\ngetAssignedVariant(assignments, experimentKey): string | null\n\n// 배정 저장\nsaveAssignment(assignments, experimentKey, variant): ExperimentAssignment[]\n\n// 이벤트 로깅 정보 생성\ncreateExperimentExposureEvent(experimentKey, variant): Record<string, any>\ncreateExperimentResultEvent(experimentKey, variant, metric, value): Record<string, any>\n\n// 모든 이벤트에 실험 정보 추가\nenrichEventWithExperiments(event, assignments): Record<string, any>\n```\n\n### 📂 `src/services/ValidationService.ts` (치트 방지)\n\n**주요 함수**:\n\n```typescript\n// 스테이지 급상승 탐지\nvalidateStageJump(previousStage, newStage, maxJump?): ValidationResult\n\n// 골드 급증 탐지\nvalidateGoldIncome(previousGold, newGold, expectedIncome, maxMultiplier?): ValidationResult\n\n// 업그레이드 레벨 검증\nvalidateUpgradeLevel(upgradeType, level, maxLevel?): ValidationResult\n\n// 타임스탬프 검증\nvalidateTimestamp(timestamp, maxDeviation?): ValidationResult\n\n// 오프라인 보상 검증\nvalidateOfflineReward(offlineTime, maxOfflineTime, expectedReward, actualReward): ValidationResult\n\n// 랭킹 제출 검증\nvalidateLeaderboardSubmission(stage, points, power, lastSubmissionTime, minInterval?): ValidationResult\n\n// Soft clamp: 값을 범위 내로 조정\nsoftClamp(value, min, max): number\n```\n\n---\n\n## (4) 수동 테스트 시나리오 (20개)\n\n| # | 시나리오 | 확인 항목 |\n|---|---|---|\n| 1 | **친구 코드 생성** | 8자리 대문자/숫자 코드 생성 확인 |\n| 2 | **친구 코드 공유** | 코드 복사 버튼 동작, 클립보드 저장 |\n| 3 | **친구 요청** | 코드 입력 → 요청 생성 → 상대방 알림 |\n| 4 | **친구 수락** | 요청 목록에서 수락 → 친구 목록에 추가 |\n| 5 | **친구 거절** | 요청 목록에서 거절 → 상태 변경 |\n| 6 | **친구 목록** | 친구 스테이지/전투력/최근 접속 표시 |\n| 7 | **정비반 생성** | 정비반 이름 입력 → 생성 → 코드 자동 생성 |\n| 8 | **정비반 가입** | 코드 입력 → 가입 신청 → 수락 |\n| 9 | **정비 포인트** | 전투/보스/미션으로 포인트 누적 확인 |\n| 10 | **정비반 보상** | 주간 목표 달성 → 토큰/칭호 지급 |\n| 11 | **주간 스테이지 랭킹** | 상위 10명 표시, 내 순위 강조 |\n| 12 | **주간 포인트 랭킹** | 정비 포인트 기준 랭킹 표시 |\n| 13 | **이벤트 랭킹** | 이벤트 ON → 랭킹 표시, OFF → 숨김 |\n| 14 | **주간 키 리셋** | 새 주차 시작 → 랭킹 자동 리셋 |\n| 15 | **배너 메시지** | 홈 상단에 배너 표시, 닫기 버튼 동작 |\n| 16 | **모달 메시지** | 중요 공지 모달 팝업, 닫기 불가 |\n| 17 | **우편함** | 메시지 목록 표시, 읽음/삭제 동작 |\n| 18 | **A/B 실험** | 사용자별 고정 variant 배정, 로깅 |\n| 19 | **치트 탐지** | 비정상 값 입력 → Soft clamp 적용 |\n| 20 | **마이그레이션 (V6→V7)** | Sprint 6 저장 데이터 로드 → 자동 마이그레이션 |\n\n---\n\n## (5) 운영 가이드\n\n### 인앱 메시지 운영\n\n**원격 설정 예시** (`RemoteConfigService`에서 로드):\n\n```json\n{\n  \"inAppMessages\": [\n    {\n      \"id\": \"msg_event_2024_01\",\n      \"type\": \"banner\",\n      \"title\": \"새해 이벤트!\",\n      \"body\": \"특별한 보상을 받아보세요~\",\n      \"startAt\": 1704067200000,\n      \"endAt\": 1704153600000,\n      \"priority\": \"high\",\n      \"ctaLabel\": \"참여\",\n      \"ctaRoute\": \"EventHub\",\n      \"isDismissible\": true\n    }\n  ]\n}\n```\n\n### A/B 실험 운영\n\n**실험 설정 예시**:\n\n```json\n{\n  \"experiments\": [\n    {\n      \"key\": \"tutorial_flow\",\n      \"name\": \"튜토리얼 최적화\",\n      \"variants\": {\n        \"A\": { \"label\": \"기본\", \"rollout\": 50 },\n        \"B\": { \"label\": \"압축\", \"rollout\": 50 }\n      },\n      \"isActive\": true,\n      \"startAt\": 1704067200000,\n      \"endAt\": 1706745600000\n    }\n  ]\n}\n```\n\n### 랭킹 운영\n\n**주간 리셋 자동화** (주간 키 기반):\n\n```typescript\n// 매주 월요일 자동으로 새 주차 시작\nconst currentWeekKey = getWeekKey(); // \"2024-01\"\nif (lastWeekKey !== currentWeekKey) {\n  // 이전 주차 보상 지급\n  awardLeaderboardRewards(leaderboard);\n  // 새 랭킹 초기화\n  leaderboard = initializeLeaderboard('weekly_stage');\n}\n```\n\n### Firebase 설정 (선택)\n\n**최소 Security Rules**:\n\n```javascript\nrules_version = '2';\nservice cloud.firestore {\n  match /databases/{database}/documents {\n    // 사용자 프로필: 자신의 데이터만 쓰기 가능\n    match /users/{userId} {\n      allow read: if true;\n      allow write: if request.auth.uid == userId && \n                      request.resource.data.keys().hasOnly(['displayName', 'stage', 'power', 'lastSeenAt']);\n    }\n    \n    // 친구 요청: 자신의 요청만 쓰기 가능\n    match /friendRequests/{docId} {\n      allow read: if request.auth.uid == resource.data.userId || \n                     request.auth.uid == resource.data.friendUserId;\n      allow create: if request.auth.uid == request.resource.data.userId;\n      allow update: if request.auth.uid == resource.data.friendUserId;\n    }\n    \n    // 랭킹: 읽기만 가능, 서버에서만 쓰기\n    match /leaderboards/{leaderboardId}/entries/{entryId} {\n      allow read: if true;\n      allow write: if false; // Cloud Functions에서만 쓰기\n    }\n  }\n}\n```\n\n---\n\n## (6) 주간 운영 체크리스트\n\n- [ ] 월요일: 랭킹 리셋 확인, 이전 주차 보상 지급\n- [ ] 수요일: 주간 이벤트 진행도 확인, 필요시 밸런스 조정\n- [ ] 금요일: 인앱 메시지 업데이트 (주말 이벤트 공지)\n- [ ] 일요일: A/B 실험 결과 분석, 다음 주 실험 계획\n- [ ] 매일: 비정상 값 로그 확인, 치트 탐지 알림 검토\n\n---\n\n이것으로 Sprint 7 개발을 완료했습니다. 커뮤니티, 랭킹, 라이브옵스 커뮤니케이션, A/B 실험, 치트 방지 시스템이 모두 준비되었으며, 게임은 완성도 높은 라이브 게임으로 진화했습니다.\n
